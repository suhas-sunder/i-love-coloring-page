# Historical test archive

These 27 files were formerly failing source-shape snapshots. They are preserved as audit evidence, renamed with `.disabled`, and intentionally excluded from every active test command.

The authoritative replacement coverage is recorded in `reports/historical-test-review.csv` and `reports/legacy-test-disposition.md`. New behavior belongs in the active focused tests and the non-pixel refinement contract, rather than in frozen implementation-fragment assertions.
