# Round 4Z Production CORS Requirements

- Production CORS required for future JPG/WebP UI: true
- Allowed methods: GET, HEAD
- Content types: image/svg+xml, image/png
- Canvas export requires an origin-clean image. The final R2/custom domain must allow GET and HEAD from the public site origin.
